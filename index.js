import React from 'react';
import { AppRegistry, StyleSheet, Text, View } from 'react-native';
import { Pages } from './src';

// Module name
AppRegistry.registerComponent('RNHighScores', () => Pages.RNHighScores);
AppRegistry.registerComponent('About', () => Pages.About);